# Prediction Engine

An initial dependency-light online prediction engine using robust online statistics. Predictions are explicitly typed, and include lower/upper bounds along with a quantifiable confidence score.

The engine relies on statistics like exponentially weighted moving averages and sample counts to infer queue delay, E2E latency, and failure probability.
