# Decision Memory

Bounded collection for storing requests, predicted metrics, utilities, and outcomes. Implemented natively in memory without requiring an unbounded database dependency. Memory is strictly bounded to prevent memory leaks in long-running deployments.
